# API
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6

# Database
pymongo>=4.6
python-dotenv>=1.0

# Retrieval — embeddings are computed locally, never sent to a third party
sentence-transformers>=2.5
rank-bm25>=0.2.2
rapidfuzz>=3.6
numpy>=1.26

# Cross-encoder reranking (Calendar category)
flashrank>=0.2

# Answer generation (Syllabus and Calendar categories)
groq>=0.11

# Shop-owner code hashing
bcrypt>=4.1

# Reviewer-facing demo UI. The production widget is static/index.html,
# served by FastAPI, and does not need this.
streamlit>=1.40

# Tracing — optional, disabled with LANGSMITH_TRACING=false
langsmith>=0.1

# API
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6

# Database
pymongo>=4.6
python-dotenv>=1.0

# Retrieval — embeddings run locally, never in the cloud
sentence-transformers>=2.5
rank-bm25>=0.2.2
rapidfuzz>=3.6
numpy>=1.26

# Shop-owner code hashing
bcrypt>=4.1

# LLM (Syllabus category only)
groq>=0.11

# Tracing (optional — the app runs with LANGSMITH_TRACING=false)
langsmith>=0.1
